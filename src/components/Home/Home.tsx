import * as React from 'react';
import { ThemeContainer } from '../../containers/themeContainer';

const Home = () => {
  return (
    <div>
      <span>home</span>
      <ThemeContainer />
    </div>
  );
};

export default Home;
