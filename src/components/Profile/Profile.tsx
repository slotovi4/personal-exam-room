import * as React from 'react';

interface IProps {
  data: any;
}

const Profile = ({ data }: IProps) => {
  return (
    <div>
      <span>profile</span>
    </div>
  );
};

export default Profile;
